version: '3'
services:
  ssh-gateway:
    image: ghcr.io/cgsdev0/rollycubes-ssh-gateway
    restart: always
    volumes:
      - ../secrets/:/secrets
    ports:
      - "3022:22"

networks:
  default:
    name: rollycubes
    external: true
