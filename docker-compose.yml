version: "3.9"  # optional since v1.27.0
services:
  game-server:
    # image: ghcr.io/cgsdev0/rollycubes:beta
    image: gameserv
    ports:
      - "3001:3001"
    volumes:
      - ./:/build
    external_links:
      - "auth:auth"
networks:
  default:
    external: true
    name: auth_default

